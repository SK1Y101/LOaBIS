LOaBIS:

1) Loading itself
    1) initialise Software
    2) fetch Module list
    3) fetch module details (including core)
        1) version
        2) name
        3) dependencies
    4) remove non functioning Modules
        1) if a dependancy is not loaded
        2) if the version is incompatible
    5) fetch module commands/functions
    6) fetch miscelaneous module details
        1) startfuncs
        2) endfuncs
        3) persistfuncs
        4) replacement functions
    7) execute startfuncs
    8) launch persistence script
    9) launch ui

2) User interface
    1) tkinter .tkk based
    2) text to speech/ speech to text compatible?
    3) terminal/gui choice
    4) small version (think taskbar) or large (think cortana) available
    5) super easy to replace/modify
    6) use the developed LOaBIS Icons

3) module creation wizard
    1) actually finish the damned thing
    2) automatic github module area creation
    3) Basically this is a super barebones atom for github type thing

4) module installer
    1) use github installation rather than dropbox
    2) maybe ui overhaul?
    3) faster loading method (downloading all the modules first is okay if there's 5 or 6 modules at most)

5) persistence module
    1) does what it says on the tin

6) move modules to github
    1) does what it says on the tin
