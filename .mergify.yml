pull_request_rules:
  - name: merge on CI success
    conditions:
      - base=master
      - label!=work-in-progress
    actions:
      merge:
        method: merge
  - name: delete head branch after merge
    conditions: []
    actions:
      delete_head_branch: {}
